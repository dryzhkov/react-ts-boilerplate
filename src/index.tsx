import * as React from 'react';
import * as ReactDOM from 'react-dom';
import {DateTimeButton} from './components/DateTimeButton';

ReactDOM.render(<DateTimeButton />, document.getElementById('root'));
